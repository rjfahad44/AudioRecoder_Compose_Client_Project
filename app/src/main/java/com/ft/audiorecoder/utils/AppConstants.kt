package com.ft.audiorecoder.utils

import com.ft.audiorecoder.BuildConfig

object AppConstants {
    const val DATABASE_NAME = BuildConfig.APPLICATION_ID + "_recorder_db"
    const val DATE_FORMAT = "dd/MM/yy"
}